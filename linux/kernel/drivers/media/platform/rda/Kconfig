config VIDEO_RDA
	tristate "RDA Camera Interface driver"
	depends on VIDEO_DEV && PLAT_RDA && SOC_CAMERA
	select VIDEOBUF2_DMA_CONTIG
	---help---
	  This is a v4l2 driver for the RDA Camera Interface

